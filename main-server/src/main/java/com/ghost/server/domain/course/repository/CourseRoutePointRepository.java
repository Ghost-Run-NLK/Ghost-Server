package com.ghost.server.domain.course.repository;

import com.ghost.server.domain.course.entity.CourseRoutePoint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CourseRoutePointRepository extends JpaRepository<CourseRoutePoint, Long> {
}
